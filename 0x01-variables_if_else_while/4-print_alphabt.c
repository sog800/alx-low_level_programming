#include <stdio.h>

/**
 * main - Entry point
 *
 * return: Always 0 (Success)
 */
int main(void)
{
	char x;

	for (x = 'a'; x <= 'z'; x++)
	{
		if (x == 'e')
			for (x = 'f'; x <= 'p'; x++)
				
				
				putchar(x);
			
	}
	putchar('\n');
	return (0);

}
