#include "main.h"
#include <string.h>
/**
 * _strlen - lenth of a string
 * @s: parameter of the string to be tasted
 *
 * Return: always int for success
 */
int _strlen(char *s)
{
	int lenNumber;

	lenNumber = strlen(s);

	return (lenNumber);
}
